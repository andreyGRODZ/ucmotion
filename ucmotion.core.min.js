<script>
/*!
 * UC Motion Core v2.0
 * Author: andreyGRODZ
 * License: MIT
 * Philosophy: layout once, animate clean
 */
(function(w,d){
'use strict';

/* =========================
   HELPERS
========================= */
const raf = ()=> new Promise(r=>requestAnimationFrame(r));
const ready = ()=> d.readyState!=='loading'
  ? Promise.resolve()
  : new Promise(r=>d.addEventListener('DOMContentLoaded',r,{once:true}));

const getAtom = (el)=>
  el?.classList?.contains('tn-atom') ? el : el?.querySelector?.('.tn-atom');

const splitWithEntities = (text)=>
  text.replace(/&[a-zA-Z0-9#]+;/g,'|$&|')
      .split('|')
      .flatMap(p=>/^&.*;$/.test(p)?[p]:[...p]);

/* =========================
   WRAP (structure only)
========================= */
function wrapText(atom, unit){
  if(atom.__ucWrapped && atom.__ucUnit===unit) return;

  const html = atom.innerHTML.replace(/\[gap\]/g,'<span data-uc-gap></span>');
  const parts = html.replace(/(<[^>]+>)/g,'|$1|').split('|').filter(Boolean);

  let out = '';
  for(const p of parts){
    if(p.startsWith('<') && p.endsWith('>')){
      out+=p; continue;
    }
    const tokens = p.split(/(\u00A0| )/);
    tokens.forEach(t=>{
      if(t===' '||t==='\u00A0'){
        out+=`<span class="uc-space">${t}</span>`;
      } else if(unit==='letters'){
        const letters = splitWithEntities(t)
          .map(ch=>`<span class="uc-letter">${ch}</span>`).join('');
        out+=`<span class="uc-word">${letters}</span>`;
      } else {
        out+=`<span class="uc-word">${t}</span>`;
      }
    });
  }

  atom.style.visibility='hidden';
  atom.innerHTML=out;

  atom.__ucWrapped=true;
  atom.__ucUnit=unit;
}

/* =========================
   BUILD LINES (layout once)
========================= */
function buildLayout(atom){
  if(atom.__ucLayout) return atom.__ucLayout;

  const nodes=[...atom.childNodes];
  const lines=[];
  let row=[], top=null;

  nodes.forEach(n=>{
    if(n.nodeType!==1) return;

    if(n.hasAttribute?.('data-uc-gap')){
      if(row.length) lines.push({nodes:row,isGap:false});
      lines.push({nodes:null,isGap:true});
      row=[]; top=null; return;
    }

    if(n.tagName==='BR') return;

    const t=n.offsetTop;
    if(top===null) top=t;
    if(t!==top){
      lines.push({nodes:row,isGap:false});
      row=[]; top=t;
    }
    row.push(n);
  });
  if(row.length) lines.push({nodes:row,isGap:false});

  atom.innerHTML='';
  lines.forEach(l=>{
    const div=d.createElement('div');
    div.className='uc-line';
    if(l.isGap){
      div.classList.add('uc-gap');
      div.innerHTML='&nbsp;';
    } else {
      l.nodes.forEach(n=>div.appendChild(n));
    }
    atom.appendChild(div);
  });

  atom.style.visibility='visible';

  atom.__ucLayout={ lines };
  return atom.__ucLayout;
}

/* =========================
   VIEWPORT OBSERVER
========================= */
function observe(el, threshold, cb){
  const io=new IntersectionObserver(ents=>{
    ents.forEach(e=>{
      if(e.isIntersecting){
        cb();
        io.unobserve(el);
      }
    });
  },{threshold});
  io.observe(el);
}

/* =========================
   CORE
========================= */
const REG=new Map();

const UCM={
  version:'2.0.0',

  register(selector,effect){
    REG.set(selector,effect);
    return this;
  },

  invalidate(el){
    const atom=getAtom(el);
    if(!atom) return;
    delete atom.__ucLayout;
  },

  init(){
    ready().then(()=>{
      REG.forEach((fx,sel)=>{
        [...d.querySelectorAll(sel)].forEach(el=>{
          if(el.__ucInit) return;
          el.__ucInit=true;

          const atom=getAtom(el);
          if(!atom) return;

          const cfg=fx.config?fx.config(el):{};
          const unit=cfg.effect||'words';

          wrapText(atom,unit);

          (async()=>{
            await raf(); await raf();
            if(d.fonts) await d.fonts.ready;

            const layout=buildLayout(atom);

            observe(el,cfg.threshold||0.4,()=>{
              fx.run({el,atom,layout,config:cfg});
            });
          })();
        });
      });
    });
  }
};

w.UCM=UCM;
})(window,document);
</script>
